The zshrc file in this directory needs to be symlinked to "~/.zshrc".

In addition, oh-my-zsh needs to be installed and the theme in ~/.config/zsh/themes directory needs to be symlinked to "~/.oh-my-zsh/themes/".
